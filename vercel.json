{
  "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && npm install && prisma generate && npm run build",
  "outputDirectory": "frontend/out",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
